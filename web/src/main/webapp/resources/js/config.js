/**
 * config.js
 * js 호출시 첫번째로 호출
 */
//console.log('config');
const _path = location.pathname
	, _fullPath = location.href
	, _realURL = location.protocol + '//' + location.host
	, _siteURL = 'http://http://localhost/:8090'
